# Generic Template - Customized During Setup

**NOTE:** This is a TEMPLATE file. Run `@workspace /total-recall` to generate a project-specific version.

**Template Variables:**
- `{{PROJECT_NAME}}` - Your project name
- `{{PROJECT_TYPE}}` - Project type (.NET, Node.js, Python, Java, etc.)
- `{{FRAMEWORKS}}` - Frameworks/libraries
- `{{BUILD_COMMAND}}` - Build command
- `{{TEST_COMMAND}}` - Test command
- `{{RUN_COMMAND}}` - Run command
- `{{LINT_COMMAND}}` - Linting command
- `{{DATABASE_TYPE}}` - Database/ORM type
- `{{DATABASE_NAME}}` - Primary database name
- `{{DATABASE_SERVER}}` - Database server
- `{{SCHEMA_PRIMARY}}` - Primary writable schema
- `{{SCHEMA_READONLY}}` - Read-only schemas (comma-separated)
- `{{CONNECTION_STRING_KEY}}` - Connection string config key
- `{{SOURCE_PATH}}` - Main source code path
- `{{TEST_PATH}}` - Test files path
- `{{CONFIG_PATH}}` - Configuration files path
- `{{TEST_FRAMEWORK}}` - Testing framework
- `{{UI_FRAMEWORK}}` - UI framework
- `{{ANALYZER_TOOLS}}` - Code analysis tools
- `{{PACKAGE_MANAGER}}` - Package manager
- `{{API_BASE_URL}}` - API base URL
- `{{REALTIME_TECH}}` - Real-time technology

---

# SelfAwareness – Global Operating Guardrails (2.5.0)

> Canonical operating rules for all agents. Keep **.github/prompts/** as the source of truth.  
> Everything else lives under **Workspaces/Copilot/**.

## 🔀 Branch Strategy (CRITICAL)

**BRANCH STRUCTURE**:
- **`master`** - Production branch (PROTECTED)
  - ALWAYS represents what's currently deployed in production
  - NEVER commit directly to master
  - Only receives merges from `development` after testing
  - Deployment script deploys from this branch
  
- **`development`** - Active development branch (DEFAULT)
  - ALL development work happens here
  - ALL feature implementations
  - ALL bug fixes
  - ALL testing and experimentation
  - Agents should ALWAYS work in this branch

**WORKFLOW**:
1. **Development**: All work done in `development` branch
2. **Testing**: Validate changes thoroughly in development
3. **Merge**: When ready for production, merge `development` → `master`
4. **Deploy**: Run deployment script which deploys from `master` branch
5. **Continue**: Resume development work in `development` branch

**ENFORCEMENT**:
- ⚠️ **NEVER** modify `master` branch directly
- ✅ **ALWAYS** create commits in `development` branch
- ✅ **ALWAYS** verify you're in `development` before starting work:
  ```{{SHELL_TYPE}}
  git branch --show-current  # Should return: development
  ```
- ❌ If on `master`, switch immediately:
  ```{{SHELL_TYPE}}
  git checkout development
  ```

**RATIONALE**:
- Production stability: `master` only contains tested, deployable code
- Safe experimentation: `development` allows iteration without affecting production
- Clear deployment path: Deployment script knows to deploy from `master`
- Easy rollback: Can revert `master` without losing development work

## Scope
Governs all agent operations including task execution, testing, cleanup, synchronization, and refactoring.

## Required Reading
**CRITICAL:** Before making any architectural decisions, implementing new features, or modifying existing code, agents **MUST** consult:
- **`.github/instructions/Links/SystemIndex.md`** - Central navigation hub for all architectural references, agent coordination, and system snapshots
- **`.github/instructions/Links/InfrastructureQuickRef.md`** - **MANDATORY** for database operations - contains {{DATABASE_NAME}} connection details and schema access rules
- **`.github/instructions/Links/Architecture.md`** - Comprehensive application architecture documentation including:
  - Complete API endpoint catalog
  - Component and page inventory
  - Service architecture with responsibilities
  - Real-time communication patterns ({{REALTIME_TECH}})
  - Data model catalog and database schemas
  - Authentication flows and security patterns
  - Integration patterns and common workflows

**Purpose:** This prevents duplication of existing functionality and ensures new implementations follow established architectural patterns.

## 🗄️ Database Access Rules (MANDATORY)

**PRIMARY DATABASE: {{DATABASE_NAME}}**
- When user mentions "database", assume **{{DATABASE_NAME}}** unless specified otherwise
- Server: {{DATABASE_SERVER}}
- Connection: Always use `{{CONNECTION_STRING_KEY}}`

**SCHEMA ACCESS CONTROL**:
- ✅ **`{{SCHEMA_PRIMARY}}`**: READ-WRITE allowed
  - Primary writable schema for application data
  
- ❌ **`{{SCHEMA_READONLY}}`**: **READ-ONLY ONLY** - NO INSERT, UPDATE, DELETE allowed
  - Reference data and legacy tables
  - Stored procedures for read operations only
  
- ❌ **All other schemas**: **READ-ONLY**

**VIOLATION CONSEQUENCES**:
- Any attempt to modify `{{SCHEMA_READONLY}}` or other READ-ONLY schemas will result in:
  - Immediate task failure
  - Rollback to checkpoint
  - User notification of violation

**See**: `.github/instructions/Links/InfrastructureQuickRef.md` for complete database documentation

## Core Principles
- **Deterministic rails**: follow these rules exactly; do not invent new flows.  
- **Single source of truth**: prompts here; configs and state under `Workspaces/Copilot/`.  
- **Evidence-first**: factor terminal logs, analyzers, and artifacts into analysis and summaries.  
- **Small steps**: change one thing at a time, accumulate tests, and stabilize before moving on.

## Phase Prompt Processing
All agents must handle `---` delimited input as separate todo items:

### Phase Recognition
- **Delimiter**: `---` on its own line indicates phase separation
- **Parsing**: Split user input into individual phases for sequential processing
- **Identification**: Each phase gets a unique identifier: `phase_{number}` where number starts at 1

### Phase Processing Workflow
For each phase, agents must:
1. **Implementation**: Make the required change for this specific phase
2. **Test Generation**: Create headless, silent test in `Workspaces/TEMP/`
   - Naming: `{agent}-phase-{phase_number}-{key}-{RUN_ID}.spec.ts`
   - Must be headless and silent (no UI)
   - Must validate the specific change made in this phase
3. **Test Validation**: Ensure test passes (retry up to 3 times if needed)
4. **Phase Completion**: Mark complete with debug log
5. **Next Phase**: Move to subsequent phase only after current phase is fully complete

### Temporary Test Management
- **Location**: All phase tests go in `Workspaces/TEMP/` directory
- **Cleanup**: Remove temporary phase tests after all phases complete (unless `commit:false`)
- **Distinction**: Permanent tests follow proper structure as per config files in `{{CONFIG_PATH}}`
- **Isolation**: Each phase test should be independent and not depend on previous phase tests

### Phase Completion Logging
- **Format**: `[DEBUG-WORKITEM:{key}:impl:{RUN_ID}] phase_{number}_complete status=success/failure ;CLEANUP_OK`
- **Required**: Each phase must log completion before proceeding to next phase
- **Failure Handling**: If phase fails, stop processing and report failure with specific phase number  

## File Organization Rules
**CRITICAL**: Never create analysis, summary, or documentation files in the project root.

### Documentation & Analysis File Placement
All agent-generated documentation must be placed in the designated directory structure:

```
Workspaces/Copilot/
├── _DOCS/                    # All analysis and summary documents
│   ├── summaries/           # Work completion summaries
│   ├── analysis/            # Technical analysis documents
│   ├── configs/             # Configuration documentation
│   └── migrations/          # Migration and reorganization docs
├── artifacts/               # Build and test artifacts
├── config/                  # Agent configurations
└── prompts.keys/           # Key-based prompt storage
```

### Code Quality Analysis Organization
**MANDATORY**: All code analysis data is strictly organized under `Workspaces/CodeQuality/`:

```
Workspaces/CodeQuality/
├── README.md                         # Comprehensive usage documentation
├── run-analyzer.{{SCRIPT_EXT}}      # Automated analysis execution script
├── CONFIGURATION_COMPLETE.md        # Setup completion status
└── Analyzer/
    ├── Config/
    │   └── analyzer.config          # Main analyzer configuration
    ├── Reports/
    │   ├── baseline-analysis.json   # Initial baseline for comparison
    │   ├── latest-analysis.json     # Most recent analysis
    │   └── analysis_*.json          # Timestamped historical reports
    └── Logs/
        ├── latest-analysis.log      # Most recent execution log
        └── analysis_*.log           # Timestamped historical logs
```

**Analyzer Execution Rules:**
- **NEVER** run analyzer commands directly in project root
- **ALWAYS** use: `{{ANALYZER_SCRIPT_PATH}}`
- **Report Access**: Latest results always at `Workspaces/CodeQuality/Analyzer/Reports/latest-analysis.json`
- **No Root Pollution**: All analysis artifacts are contained in the organized structure

**Enforcement:**
- **Summaries**: `Workspaces/Copilot/_DOCS/summaries/`
- **Analysis**: `Workspaces/Copilot/_DOCS/analysis/`
- **Config Documentation**: `Workspaces/Copilot/_DOCS/configs/`
- **Migration Reports**: `Workspaces/Copilot/_DOCS/migrations/`
- **Code Quality**: `Workspaces/CodeQuality/Analyzer/`
- **Never use project root** for any temporary or analysis files

## Absolute Runtime Rules

### For {{PROJECT_TYPE}} Application Development
- Launch via designated scripts in `Workspaces/Global/` directory
- Use appropriate build commands: `{{BUILD_COMMAND}}`
- Use appropriate run commands: `{{RUN_COMMAND}}`
- If the agent initiates a stop/restart, **self-attribute** in logs and summaries.

### For Database Access Architecture
- **CRITICAL RULE**: Database data MUST be accessed through proper architectural layers
- **UI Layer**: Follow {{UI_FRAMEWORK}} best practices for data access
- **API-First**: All database operations should go through properly designed API endpoints
- **Direct Access**: Only permitted in designated service/repository layers
- **Enforcement**: UI components must use proper HTTP clients for all data access

### For Testing with {{TEST_FRAMEWORK}}
**CRITICAL**: Testing framework has its own server management system - DO NOT manually start the app!

- **Never** manually run application before tests
- **Never** launch the app in a separate window/terminal for tests
- Testing framework manages application lifecycle **automatically** via configuration
- Use appropriate test commands: `{{TEST_COMMAND}}`
- Tests run with framework managing the application as needed

## Debug Logging Rules
- All debug lines must use the consistent marker:  
  `[DEBUG-WORKITEM:{key}:{layer}:{RUN_ID}] message ;CLEANUP_OK`  
- `{layer}` values: `impl`, `tests`, `pwtest`, `retrosync`, `refactor`, `cleanup`, `lifecycle`.  
- `RUN_ID` is a short unique id (timestamp + random suffix).  
- Behavior by mode:  
  - **none**: do not insert debug lines.  
  - **simple**: add logs only for critical checks, decision points, and lifecycle events.  
  - **trace**: log every step of the flow, including intermediate calculations, branching decisions, and results.  

## Analyzer & Linter Enforcement
All agents must enforce **{{ANALYZER_TOOLS}}** before declaring success.

### Analyzer Integration
- **Tools**: {{ANALYZER_TOOLS}}
- **Configuration**: Centralized configuration ensures:  
  - Latest analysis standards enabled
  - Code style enforcement in build
  - Warning treatment (project-specific)
- **Suppressions**: Intentionally suppressed rules per project standards
- **Invariant**: Do **not** attempt to reintroduce suppressed rules unless explicitly instructed.

### Linting Integration
- Main config: `{{LINT_CONFIG_PATH}}`
- **Baseline Debt**: Accepted technical debt patterns documented
- Agents must **not** attempt to eliminate accepted debts unless explicitly instructed.

### Package Manager Scripts
All agents must use the centralized scripts for linting, formatting, and testing:

- **Linting**: `{{LINT_COMMAND}}`
- **Formatting Check**: `{{FORMAT_CHECK_COMMAND}}`
- **Formatting Fix**: `{{FORMAT_FIX_COMMAND}}`
- **Type Check**: `{{TYPE_CHECK_COMMAND}}`
- **Tests**: `{{TEST_COMMAND}}`

**Enforcement Rules:**
- Run from repository root directory
- All scripts automatically reference centralized config files
- **Zero tolerance**: All linting and formatting must pass before declaring success
- **Error Interpretation**: Distinguish between config issues vs. actual code problems

## Modernization Guardrails
- **Imports**: All new code must use modern import syntax per {{PROJECT_TYPE}} standards
- **Typing**: Prefer explicit types over any/dynamic. If unavoidable, document why.  
- **Error Handling**: Use proper error handling patterns for resilience.  
- **Code Hygiene**: Delete unused functions/variables instead of prefixing with `_`.  
- **Formatting**: Always format using project formatter configuration

## Memory of Failures & Prohibited Retries
Agents must **record and respect failed approaches**:  
- Do not retry approaches that have been documented as failures
- Do not force rewrites when patterns are established
- Do not attempt to "fix" intentionally suppressed rules

## Test Strategy Rules
- Always generate tests incrementally:  
  1. Begin with simple checks
  2. Progress to integration tests
  3. Only at the final stage use full end-to-end scenarios
- If a test hangs, agents must self-recover and retry with reduced scope.  

## Logging for Success/Failure Memory
- On success: append `;SUCCESS_PATH` to debug logs.  
- On failure: append `;FAIL_PATH:{reason}` and mark prohibited retries.  
- Summaries must include both successes and failures so future generations don't repeat mistakes.

## Version Control Rules
- Always operate within Git.  
- On completion: ensure `git status` is clean.  
- Commits must include RUN_ID in the message for traceability.

---

## Key Infrastructure

### Configuration File Organization
All configuration files are centralized:

```
{{CONFIG_PATH}}/
├── linting/                 # Linting configurations
├── testing/                 # Test configurations
├── formatting/              # Formatter configurations
└── analysis/               # Code analysis configurations
```

**Key Points:**
- **Centralized Location**: All configs in designated directory
- **Scripts**: All package.json/build scripts reference the centralized locations
- **Path Updates**: All relative paths correctly updated for directory structure

### Application Launch & Port Management

### Database Connectivity

#### {{DATABASE_NAME}} Connection String
```
{{CONNECTION_STRING_EXAMPLE}}
```

**Connection Requirements:**
- Use specified database server and credentials
- All agents and scripts must connect to specified instance
- Connection string must be consistent across all applications

#### Application Server
- Default app port: {{DEFAULT_PORT}}
- Use proper launch scripts for port management
- Always check for orphaned processes before launch

#### ORM/Data Access
- Use {{DATABASE_TYPE}} for data access
- Connection string must match configuration
- Use retry logic for connection initialization

#### Testing Infrastructure
- Centralized under {{TEST_PATH}}
- Main config: `{{TEST_CONFIG_PATH}}`
- All scripts reference the centralized config location

#### Real-Time Communication
- Technology: {{REALTIME_TECH}}
- Handle exceptions during message parsing
- Use resilient serialization formats for updates

#### API Endpoints
- Base URL: {{API_BASE_URL}}
- **Connection String Standard**: Use identical connection strings across all applications
- **Route Conflicts**: Always check for ambiguous route patterns during development
- **Authentication Flows**: Validate token formats match expected endpoint requirements

## Key Requirements

### Authentication & Session Management
- **Token Consistency**: Maintain token validation throughout workflows
- **Multi-route Support**: Components must handle multiple route patterns
- **Error Handling**: Show user-friendly messages, not technical errors

### UI/UX Standards
- **Responsive Design**: Proper layout and sizing across devices
- **Visual Consistency**: Maintain theme and styling standards
- **Component Structure**: Follow {{UI_FRAMEWORK}} best practices

### API Integration Requirements
- **Database Integration**: Support proper database contexts
- **Validation Logic**: Implement comprehensive validation
- **Real-time Updates**: {{REALTIME_TECH}} must handle updates without connection drops

### Data Management
- **Dynamic Loading**: Support loading from database
- **Real-time Updates**: Proper real-time data synchronization
- **Proper Validation**: Validate all data operations

---

**Version:** 2.8.0  
**Last Updated:** Template Version for Portable System

---

## Reference: System Index
This instruction set references the central `SystemIndex.md`. Any structural changes must be reflected there.

## Git Backup & Rollback Discipline
- Every operation must begin by creating a backup commit.
- Undo logs must store commit hashes for rollback.
- Rollback uses `git reset --hard <hash>`.
- On completion, squash backup commits into one final commit.
