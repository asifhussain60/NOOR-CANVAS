@page "/host"
@using Microsoft.AspNetCore.Components
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the main Host Authentication page (HostLanding.razor)
        Navigation.NavigateTo("/", true);
    }
}
